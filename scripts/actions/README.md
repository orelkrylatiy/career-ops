# Actions

Future fork-owned execution layer: HH apply, ATS form filling, recruiter outreach, and action-result verification.

Discovery must not depend on this directory. Actions consume an already selected/canonical application target and return an explicit result.
