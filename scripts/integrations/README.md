# Integrations

Source-specific adapters for systems outside career-ops.

Planned areas: `telegram/`, `hh/`, and browser-only/custom career-site adapters when existing `providers/` cannot cover the source.

An integration fetches/extracts source data and returns it to the common discovery contract. It must not decide whether to apply.
